#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    vector<int> v;
    int Q;
    string command;
    int value;
    cin >> Q;
    for (int i = 0; i < Q; i++) {
        cin >> command;
        if (command == "pb") {
            cin >> value;
            v.push_back(value);
        }
        else if (command == "d") {
            cin >> value;
            v.erase(v.begin() + value);
        }
        else if (command == "sa") {
            sort(v.begin(), v.end());
        }
        else if (command == "sd") {
            sort(v.begin(), v.end(), greater<int>());
        }
        else if (command == "r") {
            reverse(v.begin(),v.end());
        }
    }
    for (int c : v) {
        cout << c << " ";
    }
}
